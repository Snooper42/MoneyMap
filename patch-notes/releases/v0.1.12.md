# MoneyMap v0.1.12

Date: 2026-05-30  
Source build ID: `v0.1.12`  
Asset token: `v0.1.12-20260530-1`

---

## Source metadata

The audited source metadata describes this build as:

> Dashboard command center redesign, Accounts premium UI, taxonomy filters, stale renderer cleanup, and cache-safe static release.

---

## Documentation update

This repository package includes a documentation overhaul for the audited `v0.1.12` app state.

Documentation changes:

- Replaced the stale root README.
- Updated the current version from `v0.1.9` to `v0.1.12`.
- Added a structured product audit.
- Rebuilt the roadmap around audit priorities.
- Converted legacy `.txt` release notes into Markdown.
- Moved version-specific notes into the `Patch Notes` folder.
- Added local preview, privacy/data, CSV import, and feature inventory docs.

---

## Known issue from audit

The Accounts view can remain visible after navigating away from Accounts. This should be treated as a P0 stabilization issue.

---

## App source changes in this documentation package

None. The documentation overhaul does not intentionally modify application source files.
