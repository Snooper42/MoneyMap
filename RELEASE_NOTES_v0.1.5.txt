MoneyMap v0.1.5

Included fixes
- Desktop Accounts chart no longer stretches vertically when hovering or scrolling.
- Hover popovers stay local-only and redraw less aggressively.
- New cleaner MoneyMap tab icon with light and dark SVG variants.
- Net worth secondary navigation is now labeled History to reduce redundancy.

Recommended test
1. Open Accounts on desktop.
2. Hover every chart point and scroll the page.
3. Confirm the chart height stays fixed.
4. Confirm the browser tab favicon uses the new lighter icon in light mode.
